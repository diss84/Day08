var total = 0;
function funcWithArg(digit) {
    return ( digit + 9) / 5;
}
    // Only change this code below
total = funcWithArg(6);
    // Only change this code above

console.log(total);
module.exports = funcWithArg;
